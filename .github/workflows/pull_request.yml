name: deploy_pr

on:
  pull_request:
    branches: [main]

env:
  BRANCH_NAME: ${{ github.event.pull_request.head.ref }}

jobs:
  sbom_test:
    runs-on: ubuntu-latest
    steps:
      - uses: NHSDigital/eps-action-sbom@aea-0000-move-to-syft
        name: SBOM test
