name: deploy_pr

on:
  pull_request:
    branches: [main]

jobs:
  sbom_scans:
    uses: NHSDigital/eps-action-sbom/.github/workflows/sbom_workflow.yml@aea-0000-move-to-syft
    with:
      BRANCH_NAME: ${{ github.event.pull_request.head.ref }}